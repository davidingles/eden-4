---
export interface Props {
	id: string
	title: string
	img: string
	three?: string
}

const { id, title, img } = Astro.props
---

<a href=`/${id}`>
	<img
		class='aspect-[500/500] object-cover'
		src=`${img}`
		alt=`${title}`
	/>
</a>
